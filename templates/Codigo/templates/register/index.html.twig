{% extends 'base.html.twig' %}

{% block title %}{{ 'auth.register.title'|trans }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/register.css') }}">
{% endblock %}

{% block body %}
    {# h1 oculto visualmente pero accesible para lectores de pantalla #}
    <h1>{{ 'auth.register.title'|trans }}</h1>

    <main>
        <div class="portada">
            <figure>
                <img src="{{ asset('images/logo-transparent.png') }}" alt="O Porto do Sabor">
            </figure>
        </div>

        <section>
            <header style="display:flex;justify-content:flex-end;width:100%;">
                {% include 'components/_locale_switcher.html.twig' %}
            </header>

            <h2>{{ 'register.intro'|trans }}</h2>
            {% if isAdmin %}
                <h3>{{ 'register.employee.title'|trans }}</h3>
                <p class="sub">{{ 'register.employee.subtitle'|trans }}</p>
            {% else %}
                <h3>{{ 'auth.register.title'|trans }}</h3>
                <p class="sub">{{ 'register.client.subtitle'|trans }}</p>
            {% endif %}

            {{ form_start(form) }}
                {% for field in form %}
                    {% if not field.rendered and field.vars.name != '_token' %}
                    <div class="form-row">
                        {{ form_label(field) }}
                        {{ form_widget(field) }}
                        {{ form_errors(field) }}
                    </div>
                    {% endif %}
                {% endfor %}
                <button type="submit" class="btn">
                    {% if isAdmin %}{{ 'register.employee.submit'|trans }}{% else %}{{ 'auth.register.submit'|trans }}{% endif %}
                </button>
            {{ form_end(form) }}

            {% if isAdmin %}
                <a href="{{ path('app_admin') }}" class="back">{{ 'register.back_to_admin'|trans }}</a>
            {% else %}
                <a href="{{ path('app_acceso') }}" class="back">{{ 'auth.register.has_account'|trans }}</a>
            {% endif %}
        </section>
    </main>
{% endblock %}
